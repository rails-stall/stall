= resources_table(model: model, component: component) do |table|
  = table.header do
    = table.header_for(:name)
    = table.header_for(:logo)

  = table.rows(resources) do |resource|
    = table.data_for(resource, :name)
    = table.data_for(resource, :logo)
